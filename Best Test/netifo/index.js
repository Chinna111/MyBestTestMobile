/**
 * @format
 */

import { AppRegistry } from 'react-native';
import NetInfotest from './src/NetInfotest';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => NetInfotest);
